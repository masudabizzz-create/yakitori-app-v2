-- ============================================================
-- 003_seed_data.sql
-- 初期データ投入（初回セットアップ時に1回だけ実行）
-- ============================================================

-- デフォルトテナント（1店舗目）
INSERT INTO tenants (id, name)
VALUES ('00000000-0000-0000-0000-000000000001', '串焼き本店')
ON CONFLICT (id) DO NOTHING;

-- デフォルト設定
INSERT INTO settings (tenant_id)
VALUES ('00000000-0000-0000-0000-000000000001')
ON CONFLICT (tenant_id) DO NOTHING;

-- ------------------------------------------------------------
-- 管理者ユーザーの作成手順（このSQLでは自動作成しない）
-- ------------------------------------------------------------
-- 1. Supabase Dashboard > Authentication > Users で
--    新規ユーザー（メール/パスワード）を作成する
--
-- 2. 作成されたユーザーの UUID を控え、SQL Editor で以下を実行する:
--
--    INSERT INTO public.users (id, tenant_id, name, role, is_active)
--    VALUES (
--      '<supabase-auth-user-uuid>',
--      '00000000-0000-0000-0000-000000000001',
--      '管理者',
--      'admin',
--      true
--    );
--
-- 3. 以降、運用管理(/admin/sys)からスタッフを追加できる
-- ------------------------------------------------------------
